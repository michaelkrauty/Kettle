package me.michaelkrauty.Kettle.command.factions;

import me.michaelkrauty.Kettle.Kettle;
import org.bukkit.ChatColor;
import org.bukkit.entity.Player;

/**
 * Created on 7/2/2014.
 *
 * @author michaelkrauty
 */
public class CreateCommand {

	public CreateCommand(Kettle kettle, Player player, String[] args) {
		//if (kettle.getFaction(args[0]) != null) {
		//	player.sendMessage(ChatColor.RED + "The faction " + args[0] + "already exists.");
		//	return;
		//}
	}
}
